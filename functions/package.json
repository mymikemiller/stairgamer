{
  "name": "stairgamer-functions",
  "version": "1.0.0",
  "main": "lib/index.js",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.73.0",
    "busboy": "^1.6.0",
    "exif-reader": "^2.0.2",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.3.2",
    "heic-convert": "^2.1.0",
    "sharp": "^0.35.3",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/busboy": "^1.5.4",
    "@types/heic-convert": "^2.1.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
